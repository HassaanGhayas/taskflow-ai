# SpecifyPlus MCP Server Configuration

# Path to commands directory (relative to server working directory)
COMMANDS_DIR=.claude/commands

# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# MCP transport protocol (stdio or sse)
MCP_TRANSPORT=stdio

# Port for SSE transport (only used if MCP_TRANSPORT=sse)
MCP_PORT=8080
