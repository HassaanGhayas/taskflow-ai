---
description: "A command without $ARGUMENTS placeholder"
---

# Command Without Arguments

This command doesn't have an $ARGUMENTS section.

## Usage

When invoked, it should return the content as-is without any substitution.

## Details

This tests FR-008 which specifies that commands without $ARGUMENTS should return the template unchanged.
