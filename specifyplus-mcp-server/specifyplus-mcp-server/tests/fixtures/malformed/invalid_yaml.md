---
description: "Invalid YAML with unclosed quote
handoffs:
  - label: "Broken
    agent: "sp.test"
    prompt: "This YAML is malformed"

# Content continues but YAML is broken
This should be skipped with a warning.
